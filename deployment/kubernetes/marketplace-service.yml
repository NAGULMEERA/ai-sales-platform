apiVersion: apps/v1
kind: Deployment
metadata:
  name: marketplace-service
  namespace: aisales
spec:
  replicas: 1
  selector:
    matchLabels:
      app: marketplace-service
  template:
    metadata:
      labels:
        app: marketplace-service
    spec:
      containers:
        - name: marketplace-service
          image: aisales/.0.0-SNAPSHOT
          ports:
            - containerPort: 8098
---
apiVersion: v1
kind: Service
metadata:
  name: marketplace-service
  namespace: aisales
spec:
  selector:
    app: marketplace-service
  ports:
    - port: 80
      targetPort: 8098
