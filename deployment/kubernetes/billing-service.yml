apiVersion: apps/v1
kind: Deployment
metadata:
  name: billing-service
  namespace: aisales
spec:
  replicas: 1
  selector:
    matchLabels:
      app: billing-service
  template:
    metadata:
      labels:
        app: billing-service
    spec:
      containers:
        - name: billing-service
          image: aisales/.0.0-SNAPSHOT
          ports:
            - containerPort: 8091
---
apiVersion: v1
kind: Service
metadata:
  name: billing-service
  namespace: aisales
spec:
  selector:
    app: billing-service
  ports:
    - port: 80
      targetPort: 8091
