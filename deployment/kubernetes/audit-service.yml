apiVersion: apps/v1
kind: Deployment
metadata:
  name: audit-service
  namespace: aisales
spec:
  replicas: 1
  selector:
    matchLabels:
      app: audit-service
  template:
    metadata:
      labels:
        app: audit-service
    spec:
      containers:
        - name: audit-service
          image: aisales/.0.0-SNAPSHOT
          ports:
            - containerPort: 8096
---
apiVersion: v1
kind: Service
metadata:
  name: audit-service
  namespace: aisales
spec:
  selector:
    app: audit-service
  ports:
    - port: 80
      targetPort: 8096
