apiVersion: apps/v1
kind: Deployment
metadata:
  name: media-service
  namespace: aisales
spec:
  replicas: 1
  selector:
    matchLabels:
      app: media-service
  template:
    metadata:
      labels:
        app: media-service
    spec:
      containers:
        - name: media-service
          image: aisales/.0.0-SNAPSHOT
          ports:
            - containerPort: 8095
---
apiVersion: v1
kind: Service
metadata:
  name: media-service
  namespace: aisales
spec:
  selector:
    app: media-service
  ports:
    - port: 80
      targetPort: 8095
