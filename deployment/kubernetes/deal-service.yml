apiVersion: apps/v1
kind: Deployment
metadata:
  name: deal-service
  namespace: aisales
spec:
  replicas: 1
  selector:
    matchLabels:
      app: deal-service
  template:
    metadata:
      labels:
        app: deal-service
    spec:
      containers:
        - name: deal-service
          image: aisales/.0.0-SNAPSHOT
          ports:
            - containerPort: 8097
---
apiVersion: v1
kind: Service
metadata:
  name: deal-service
  namespace: aisales
spec:
  selector:
    app: deal-service
  ports:
    - port: 80
      targetPort: 8097
