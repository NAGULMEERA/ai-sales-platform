apiVersion: apps/v1
kind: Deployment
metadata:
  name: appointment-service
  namespace: aisales
spec:
  replicas: 1
  selector:
    matchLabels:
      app: appointment-service
  template:
    metadata:
      labels:
        app: appointment-service
    spec:
      containers:
        - name: appointment-service
          image: aisales/.0.0-SNAPSHOT
          ports:
            - containerPort: 8087
---
apiVersion: v1
kind: Service
metadata:
  name: appointment-service
  namespace: aisales
spec:
  selector:
    app: appointment-service
  ports:
    - port: 80
      targetPort: 8087
