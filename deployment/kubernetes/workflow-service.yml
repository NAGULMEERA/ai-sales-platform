apiVersion: apps/v1
kind: Deployment
metadata:
  name: workflow-service
  namespace: aisales
spec:
  replicas: 1
  selector:
    matchLabels:
      app: workflow-service
  template:
    metadata:
      labels:
        app: workflow-service
    spec:
      containers:
        - name: workflow-service
          image: aisales/.0.0-SNAPSHOT
          ports:
            - containerPort: 8089
---
apiVersion: v1
kind: Service
metadata:
  name: workflow-service
  namespace: aisales
spec:
  selector:
    app: workflow-service
  ports:
    - port: 80
      targetPort: 8089
