# Automation

CI/CD automation assets.

```
automation/
├── jenkins/              # Jenkins pipeline definitions
└── workflows-reference/  # Reference copies of GitHub Actions workflows
```

Primary CI runs from `.github/workflows/` at the repository root.
