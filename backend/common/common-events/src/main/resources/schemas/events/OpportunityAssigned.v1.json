{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://aisales.com/schemas/events/OpportunityAssigned.v1.json",
  "title": "OpportunityAssigned v1",
  "type": "object",
  "additionalProperties": true,
  "required": ["eventId", "eventType", "eventVersion", "tenantId", "aggregateId", "occurredAt", "correlationId", "customerId", "assignedTo"],
  "properties": {
    "eventId": {"type": "string", "minLength": 1},
    "eventType": {"const": "OpportunityAssigned"},
    "eventVersion": {"const": 1},
    "tenantId": {"type": "string", "minLength": 1},
    "aggregateId": {"type": "string", "minLength": 1},
    "occurredAt": {"type": "string", "format": "date-time"},
    "correlationId": {"type": "string", "minLength": 1},
    "customerId": {"type": "string", "minLength": 1},
    "assignedTo": {"type": "string", "minLength": 1},
    "previousAssignedTo": {"type": "string"}
  }
}
