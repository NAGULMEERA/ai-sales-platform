{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://aisales.com/schemas/events/TenantSuspended.v1.json",
  "title": "TenantSuspended v1",
  "type": "object",
  "additionalProperties": true,
  "required": ["eventId", "eventType", "eventVersion", "tenantId", "aggregateId", "occurredAt", "correlationId", "tenantName", "slug"],
  "properties": {
    "eventId": {"type": "string", "minLength": 1},
    "eventType": {"const": "TenantSuspended"},
    "eventVersion": {"const": 1},
    "tenantId": {"type": "string", "minLength": 1},
    "aggregateId": {"type": "string", "minLength": 1},
    "occurredAt": {"type": "string", "format": "date-time"},
    "correlationId": {"type": "string", "minLength": 1},
    "tenantName": {"type": "string"},
    "slug": {"type": "string"}
  }
}
