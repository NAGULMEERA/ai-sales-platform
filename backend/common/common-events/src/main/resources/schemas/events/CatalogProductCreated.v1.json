{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://aisales.com/schemas/events/CatalogProductCreated.v1.json",
  "title": "CatalogProductCreated v1",
  "type": "object",
  "additionalProperties": true,
  "required": ["eventId", "eventType", "eventVersion", "tenantId", "aggregateId", "occurredAt", "correlationId", "code", "name", "productType", "status"],
  "properties": {
    "eventId": {"type": "string", "minLength": 1},
    "eventType": {"const": "CatalogProductCreated"},
    "eventVersion": {"const": 1},
    "tenantId": {"type": "string", "minLength": 1},
    "aggregateId": {"type": "string", "minLength": 1},
    "occurredAt": {"type": "string", "format": "date-time"},
    "correlationId": {"type": "string", "minLength": 1},
    "code": {"type": "string"},
    "name": {"type": "string"},
    "productType": {"type": "string"},
    "status": {"type": "string"}
  }
}
