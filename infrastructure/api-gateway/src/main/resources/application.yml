server:
  port: 8080

spring:
  application:
    name: api-gateway
  config:
    import: optional:configserver:http://localhost:8888
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
          lower-case-service-id: true

aisales:
  security:
    jwt:
      # RS256 public key PEM (classpath for local; production: JWT_PUBLIC_KEY_PEM secret).
      public-key-location: ${JWT_PUBLIC_KEY_PEM:${JWT_PUBLIC_KEY_LOCATION:classpath:jwt/local-public.pem}}
  gateway:
    rate-limit:
      enabled: true
      auth:
        # Steady-state login/register/refresh budget per client IP (token bucket via Redis).
        replenish-rate: 5
        burst-capacity: 10
      password-reset:
        # Stricter budget for forgot/reset/resend flows to reduce enumeration and email abuse.
        replenish-rate: 2
        burst-capacity: 5

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

management:
  endpoints:
    web:
      exposure:
        include: health,info,gateway
